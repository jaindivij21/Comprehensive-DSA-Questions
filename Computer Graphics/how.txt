// https://github.com/ullas-i/Solutions

Get all the materal needed graphics.h, winbgim.h and libbgi.a

1. If you dont have MinGW install it from official and follow the steps Copy graphics.h and winbgim.h files to MinGW/include folder. Location Might be ("C:\MinGW\include") Copy libbgi.a to file to MinGW/lib folder. Location ("C:\MinGW\lib") Open Vs Code Smart way:
2. Select a folder and open Terminal
	mkdir testFolder
	cd testFolder	
	code .

    //create a file name it (example.cpp)

    #include <graphics.h> 
      int main()
      {
          int gd = DETECT, gm;
          char data[] = "C:\\MinGW\\lib\\libbgi.a";
      
          initgraph(&gd, &gm, data);
          circle(200, 200, 100);
          getch();
          closegraph();
          return 0;
      }

Running the file using:
     g++ -o testfile filename.cpp -lbgi -lgdi32 -lcomdlg32 -luuid -loleaut32 -lole32
    //after u'll get textFile.exe run using command: .\testFile